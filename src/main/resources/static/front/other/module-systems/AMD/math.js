/**
 * 定义函数：https://requirejs.org/docs/api.html#deffunc
 */
define(function () {
  return {
    plus: (a, b) => a + b,
    subtract: (a, b) => a - b,
    multiply: (a, b) => a * b,
    divide: (a, b) => a / b
  }
})
