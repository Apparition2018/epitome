ssm-1           Java高并发秒杀
                SSM + mysql + cp30 + jsp + jstl + logback +
                mybatis-generator + 声明式事务 + redis(jedis) + 存储过程 + 通用返回对象 + protostuff + entity|dto +
                枚举 + result接口

permission      Java开发企业级权限管理系统
                SM + mysql + druid + jsp + mustache + Bootstrap(Dual Listbox) + ztree + logback +
                mybatis-generator + redis(jedis) + 通用返回对象 + 分页 + 异常处理 + Validator + jackson + param|model|dto +
                Interceptor + Filter(Login|AclControl) + ThreadLocal + Spring上下文工具(ApplicationContextHelper) +
                操作记录 + Comparator + stream + 枚举 + MD5 + 获取IP + 发送邮件 + 随机生成密码 + 随机生成code

springboot-1    SpringBoot+MyBatis搭建迷你小程序
                SM + mysql + c3p0 + 微信小程序 + 异常处理 + 单元测试(断言)

springboot-2    SpringBoot构建电商基础秒杀项目
                SM + mysql + druid + html + metronic +
                mybatis-generator + 通用返回对象 + 异常处理 + Validator(优化) + model|do|vo +
                跨域 + session + joda-time

springboot-3    SpringBoot开发常用技术整合
                SM + mysql + druid + freemarker|thymeleaf(国际化i18n) +
                MyBatisGenerator* + 通用Maper + 事务 + pagehelper + redis + 通用返回对象 + jackson +
                异常处理(web|ajax) + 资源文件读取 + server|tomcat配置 + 定时任务 + 异步任务 + Interceptor +
                idWorker

mybatis         通过自动回复机器人学Mybatis---基础版|加强版
                servlet + mysql + jdbc/mybatis + jsp + log4j +
                接口式编程 + 代理模式 + mybatis拦截器实现分页 + 数组参数(看源码的思路)

mybatis-plus    Mybatis-Plus入门
                SM + mysql + logging
                mybatis-plus + mybatis-plus分页 + 单元测试(断言)



ssh-2           基于SSH实现员工管理系统之案例实现篇
                SSH + mysql + c3p0 + jsp + log4j2 + 分页查询
                还有最后2节课没看完